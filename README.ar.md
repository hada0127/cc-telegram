# cc-telegram

🌍 **Language / 언어 / 语言**:
[English](README.md) | [한국어](README.ko.md) | [中文](README.zh.md) | [Español](README.es.md) | [हिन्दी](README.hi.md) | [العربية](README.ar.md) | [Português](README.pt.md) | [Русский](README.ru.md) | [日本語](README.ja.md) | [Français](README.fr.md) | [Deutsch](README.de.md)

---

تنفيذ Claude Code عن بُعد عبر بوت تيليجرام.

تحكم في Claude Code من أي مكان باستخدام تطبيق تيليجرام. أنشئ المهام، وراقب التقدم، واستلم إشعارات الإكمال - كل ذلك من هاتفك.

## الميزات

- **تنفيذ المهام عن بُعد**: أرسل مهام البرمجة إلى Claude Code عبر تيليجرام
- **التنفيذ المتوازي**: تشغيل مهام متعددة في وقت واحد (قابل للتكوين)
- **نظام الأولويات**: مستويات الأولوية العاجلة، العالية، العادية، المنخفضة
- **إعادة المحاولة التلقائية**: إعادة المحاولة تلقائياً عند الفشل مع محاولات قابلة للتكوين
- **الحالة في الوقت الفعلي**: مراقبة تقدم المهام ومخرجات Claude
- **تدوير السجلات**: تنظيف تلقائي للسجلات القديمة والمهام المكتملة

## المتطلبات

- Node.js 18.0.0 أو أعلى
- [Claude Code CLI](https://claude.ai/claude-code) مثبت ومصادق عليه
- حساب تيليجرام

## التثبيت

```bash
npx cc-telegram
```

أو التثبيت العام:

```bash
npm install -g cc-telegram
cc-telegram
```

## الإعداد الأولي

عند التشغيل الأول، سيرشدك cc-telegram خلال عملية الإعداد:

1. **إنشاء بوت تيليجرام**
   - افتح تيليجرام وابحث عن [@BotFather](https://t.me/BotFather)
   - أرسل `/newbot` واتبع التعليمات
   - انسخ رمز البوت المقدم

2. **إدخال رمز البوت**
   - الصق رمز البوت عند الطلب
   - ستتحقق الأداة من صلاحية الرمز

3. **ربط حسابك**
   - افتح البوت الجديد في تيليجرام
   - أرسل `/start` إلى البوت
   - سيكتشف CLI رسالتك ويعرض معرف المحادثة الخاص بك
   - أدخل معرف المحادثة للتأكيد

4. **تكوين الإعدادات**
   - حدد عدد المحاولات الافتراضي (مستحسن: 15)
   - تمكين/تعطيل التنفيذ المتوازي
   - حدد الحد الأقصى للمهام المتزامنة (إذا تم تمكين التوازي)

يتم تخزين التكوين محلياً في `.cc-telegram/config.json` (مشفر).

## الاستخدام

بعد الإعداد، ببساطة قم بتشغيل:

```bash
npx cc-telegram
```

سيبدأ البوت ويستمع للأوامر من حسابك على تيليجرام.

## أوامر تيليجرام

| الأمر | الوصف |
|-------|-------|
| `/new` | إنشاء مهمة جديدة |
| `/list` | عرض المهام المعلقة والجارية |
| `/completed` | عرض المهام المكتملة |
| `/failed` | عرض المهام الفاشلة |
| `/status` | التحقق من حالة التنفيذ الحالية |
| `/debug` | عرض معلومات النظام |
| `/cancel` | إلغاء تدفق إنشاء المهمة |
| `/reset` | إعادة تعيين جميع البيانات (مع التأكيد) |

## إنشاء المهام

### المهام البسيطة
للتنفيذ مرة واحدة بدون معايير الإكمال:

1. أرسل `/new`
2. اختر "بسيط (بدون معايير إكمال، بدون إعادة محاولة)"
3. أدخل متطلباتك
4. يتم وضع المهمة في قائمة الانتظار فوراً

### المهام المعقدة
للمهام ذات معايير الإكمال وإعادة المحاولة التلقائية:

1. أرسل `/new`
2. اختر "معقد (مع معايير إكمال وإعادة محاولة)"
3. أدخل متطلباتك
4. أدخل معايير الإكمال (مثل: "جميع الاختبارات تمر")
5. اختر مستوى الأولوية
6. اختر عدد المحاولات (10 أو مخصص)

## أولوية المهام

يتم تنفيذ المهام حسب ترتيب الأولوية:

| الأولوية | الأيقونة | الوصف |
|----------|---------|-------|
| عاجل | 🔴 | تنفيذ أولاً |
| عالي | 🟠 | أولوية عالية |
| عادي | 🟢 | الأولوية الافتراضية |
| منخفض | 🔵 | تنفيذ عند الخمول |

## التنفيذ المتوازي

عند التمكين أثناء الإعداد، يمكن تشغيل مهام متعددة في وقت واحد:

- تكوين الحد الأقصى للمهام المتزامنة (1-10)
- كل مهمة تعرض بادئة المعرف الخاص بها في مخرجات وحدة التحكم
- `/status` يعرض جميع المهام الجارية
- المهام ذات الأولوية الأعلى لا تزال تحصل على فتحات أولاً

### مخرجات وحدة التحكم (الوضع المتوازي)

```
[a1b2c3d4] بدء المهمة...
[e5f6g7h8] تجميع المشروع...
[a1b2c3d4] الاختبارات ناجحة!
```

## التكوين

يتم تخزين التكوين في `.cc-telegram/config.json`:

| الإعداد | الوصف | الافتراضي |
|---------|-------|-----------|
| `botToken` | رمز بوت تيليجرام (مشفر) | - |
| `chatId` | معرف محادثة تيليجرام الخاص بك (مشفر) | - |
| `debugMode` | تمكين تسجيل التصحيح | `false` |
| `claudeCommand` | أمر Claude CLI مخصص | `null` (اكتشاف تلقائي) |
| `logRetentionDays` | أيام الاحتفاظ بملفات السجل | `7` |
| `defaultMaxRetries` | عدد المحاولات الافتراضي | `15` |
| `parallelExecution` | تمكين التنفيذ المتوازي | `false` |
| `maxParallel` | الحد الأقصى للمهام المتزامنة | `3` |

### أمر Claude مخصص

إذا تم تثبيت Claude CLI في موقع غير قياسي:

```json
{
  "claudeCommand": "npx @anthropic-ai/claude-code"
}
```

## هيكل الدليل

```
.cc-telegram/
├── config.json      # التكوين المشفر
├── tasks.json       # فهرس المهام المعلقة
├── completed.json   # فهرس المهام المكتملة
├── failed.json      # فهرس المهام الفاشلة
├── tasks/           # ملفات المهام الفردية
├── completed/       # تفاصيل المهام المكتملة
├── failed/          # تفاصيل المهام الفاشلة
└── logs/            # ملفات السجل اليومية
```

## اكتشاف الإكمال

يشير Claude Code إلى إكمال المهمة باستخدام علامات خاصة:

- `<promise>COMPLETE</promise>` - اكتملت المهمة بنجاح
- `<promise>FAILED</promise>` - فشلت المهمة مع السبب

إذا لم يتم اكتشاف إشارة، يستخدم النظام مطابقة الأنماط لتحديد النجاح أو الفشل بناءً على محتوى المخرجات.

## إدارة السجلات

- يتم إنشاء ملفات السجل يومياً: `YYYY-MM-DD.log`
- يتم حذف السجلات القديمة تلقائياً بعد `logRetentionDays`
- يتم تنظيف ملفات المهام المكتملة/الفاشلة بعد 30 يوماً

## الأمان

- رمز البوت ومعرف المحادثة مشفران باستخدام AES-256-GCM
- يتم معالجة الرسائل فقط من معرف المحادثة المسجل الخاص بك
- جميع البيانات مخزنة محلياً في دليل مشروعك

## استكشاف الأخطاء وإصلاحها

### البوت لا يستجيب
- تأكد من أن البوت يعمل (`npx cc-telegram`)
- تحقق مما إذا كان معرف المحادثة الخاص بك يطابق المكوّن
- تحقق من اتصال الإنترنت

### لم يتم العثور على Claude Code
- تأكد من تثبيت Claude CLI: `npm install -g @anthropic-ai/claude-code`
- أو حدد أمراً مخصصاً في التكوين: `"claudeCommand": "npx @anthropic-ai/claude-code"`

### المهام عالقة في التقدم
- عند إعادة التشغيل، يتم إعادة تعيين المهام اليتيمة تلقائياً إلى حالة "ready"
- استخدم `/reset` لمسح جميع البيانات إذا لزم الأمر

## الترخيص

MIT
