# cc-telegram

[![npm version](https://badge.fury.io/js/cc-telegram.svg)](https://www.npmjs.com/package/cc-telegram)
[![GitHub](https://img.shields.io/github/license/hada0127/cc-telegram)](https://github.com/hada0127/cc-telegram)

🌍 **Language / 언어 / 语言**:
[English](README.md) | [한국어](README.ko.md) | [中文](README.zh.md) | [Español](README.es.md) | [हिन्दी](README.hi.md) | [العربية](README.ar.md) | [Português](README.pt.md) | [Русский](README.ru.md) | [日本語](README.ja.md) | [Français](README.fr.md) | [Deutsch](README.de.md)

**GitHub**: [https://github.com/hada0127/cc-telegram](https://github.com/hada0127/cc-telegram)

---

Удалённое выполнение Claude Code через Telegram-бота.

Управляйте Claude Code из любого места с помощью приложения Telegram. Создавайте задачи, отслеживайте прогресс и получайте уведомления о завершении — всё с вашего телефона.

## Возможности

- **Удалённое выполнение задач**: Отправляйте задачи по программированию в Claude Code через Telegram
- **Параллельное выполнение**: Запускайте несколько задач одновременно (настраивается)
- **Система приоритетов**: Уровни приоритета Срочный, Высокий, Обычный, Низкий
- **Автоматический повтор**: Автоматический повтор при сбое с настраиваемым количеством попыток
- **Статус в реальном времени**: Отслеживайте прогресс задач и вывод Claude
- **Ротация логов**: Автоматическая очистка старых логов и завершённых задач

## Требования

- Node.js 18.0.0 или выше
- [Claude Code CLI](https://claude.ai/claude-code) установлен и авторизован
- Аккаунт Telegram

## Установка

```bash
npx cc-telegram
```

Или глобальная установка:

```bash
npm install -g cc-telegram
cc-telegram
```

## Начальная настройка

При первом запуске cc-telegram проведёт вас через процесс настройки:

1. **Создание Telegram-бота**
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте `/newbot` и следуйте инструкциям
   - Скопируйте предоставленный токен бота

2. **Ввод токена бота**
   - Вставьте токен бота при запросе
   - Инструмент проверит валидность токена

3. **Привязка аккаунта**
   - Откройте нового бота в Telegram
   - Отправьте `/start` боту
   - CLI обнаружит ваше сообщение и покажет ваш chat ID
   - Введите chat ID для подтверждения

4. **Настройка параметров**
   - Установите количество повторов по умолчанию (рекомендуется: 15)
   - Включите/отключите параллельное выполнение
   - Установите максимум одновременных задач (если параллельное включено)

Конфигурация хранится локально в `.cc-telegram/config.json` (зашифровано).

## Использование

После настройки просто запустите:

```bash
npx cc-telegram
```

Бот запустится и будет ожидать команды от вашего аккаунта Telegram.

## Команды Telegram

| Команда | Описание |
|---------|----------|
| `/new` | Создать новую задачу |
| `/list` | Просмотр ожидающих и выполняющихся задач |
| `/completed` | Просмотр завершённых задач |
| `/failed` | Просмотр неудавшихся задач |
| `/status` | Проверить текущий статус выполнения и отменить выполняющиеся задачи |
| `/debug` | Просмотр системной информации |
| `/cancel` | Отменить процесс создания задачи |
| `/reset` | Сбросить все данные (с подтверждением) |

## Создание задач

### Простые задачи
Для однократного выполнения без критериев завершения:

1. Отправьте `/new`
2. Выберите "Простая (без критериев завершения, без повтора)"
3. Введите требование
4. Задача сразу добавляется в очередь

### Сложные задачи
Для задач с критериями завершения и автоматическим повтором:

1. Отправьте `/new`
2. Выберите "Сложная (с критериями завершения и повтором)"
3. Введите требование
4. Введите критерии завершения (например: "Все тесты проходят")
5. Выберите уровень приоритета
6. Выберите количество повторов (10 или произвольное)

### Вложения файлов

Вы можете прикреплять файлы при вводе требований или критериев завершения:

1. При запросе требований/критериев сначала отправьте файлы (изображения, документы и т.д.)
2. Для каждого прикрепленного файла появится сообщение подтверждения
3. Затем введите требования/критерии текстом
4. Прикрепленные файлы будут переданы Claude вместе с задачей

**Примечание**: Прикрепленные файлы автоматически удаляются при завершении, неудаче или отмене задачи.

## Приоритет задач

Задачи выполняются в порядке приоритета:

| Приоритет | Иконка | Описание |
|-----------|--------|----------|
| Срочный | 🔴 | Выполнить первым |
| Высокий | 🟠 | Высокий приоритет |
| Обычный | 🟢 | Приоритет по умолчанию |
| Низкий | 🔵 | Выполнить при простое |

## Параллельное выполнение

При включении во время настройки несколько задач могут выполняться одновременно:

- Настройте максимум одновременных задач (1-10)
- Каждая задача показывает свой ID-префикс в консольном выводе
- `/status` показывает все выполняющиеся задачи с кнопками остановки для их отмены
- Задачи с более высоким приоритетом всё равно получают слоты первыми

### Отмена Выполняющихся Задач

Вы можете отменить задачи, которые в данный момент выполняются:

1. Отправьте `/status` для просмотра выполняющихся задач
2. Каждая выполняющаяся задача отображает кнопку "Стоп"
3. Нажмите кнопку, чтобы немедленно завершить задачу
4. Отменённая задача будет помечена как неудавшаяся

### Консольный вывод (параллельный режим)

```
[a1b2c3d4] Запуск задачи...
[e5f6g7h8] Компиляция проекта...
[a1b2c3d4] Тесты пройдены!
```

## Конфигурация

Конфигурация хранится в `.cc-telegram/config.json`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `botToken` | Токен Telegram-бота (зашифрован) | - |
| `chatId` | Ваш Telegram chat ID (зашифрован) | - |
| `debugMode` | Включить отладочное логирование | `false` |
| `claudeCommand` | Пользовательская команда Claude CLI | `null` (автоопределение) |
| `logRetentionDays` | Дней хранения лог-файлов | `7` |
| `defaultMaxRetries` | Количество повторов по умолчанию | `15` |
| `parallelExecution` | Включить параллельное выполнение | `false` |
| `maxParallel` | Максимум одновременных задач | `3` |

### Пользовательская команда Claude

Если Claude CLI установлен в нестандартном месте:

```json
{
  "claudeCommand": "npx @anthropic-ai/claude-code"
}
```

## Структура каталогов

```
.cc-telegram/
├── config.json      # Зашифрованная конфигурация
├── tasks.json       # Индекс ожидающих задач
├── completed.json   # Индекс завершённых задач
├── failed.json      # Индекс неудавшихся задач
├── tasks/           # Файлы отдельных задач
├── completed/       # Детали завершённых задач
├── failed/          # Детали неудавшихся задач
└── logs/            # Ежедневные лог-файлы
```

## Обнаружение завершения

Claude Code сигнализирует о завершении задачи специальными маркерами:

- `<promise>COMPLETE</promise>` - Задача успешно завершена
- `<promise>FAILED</promise>` - Задача не выполнена с указанием причины

Если сигнал не обнаружен, система использует сопоставление шаблонов для определения успеха или неудачи на основе содержимого вывода.

## Управление логами

- Лог-файлы создаются ежедневно: `YYYY-MM-DD.log`
- Старые логи автоматически удаляются после `logRetentionDays`
- Файлы завершённых/неудавшихся задач очищаются через 30 дней

## Безопасность

- Токен бота и chat ID зашифрованы с использованием AES-256-GCM
- Обрабатываются только сообщения от вашего зарегистрированного chat ID
- Все данные хранятся локально в каталоге вашего проекта

## Устранение неполадок

### Бот не отвечает
- Убедитесь, что бот запущен (`npx cc-telegram`)
- Проверьте, совпадает ли ваш chat ID с настроенным
- Проверьте подключение к интернету

### Claude Code не найден
- Убедитесь, что Claude CLI установлен: `npm install -g @anthropic-ai/claude-code`
- Или установите пользовательскую команду в config: `"claudeCommand": "npx @anthropic-ai/claude-code"`

### Задачи застряли в процессе выполнения
- При перезапуске осиротевшие задачи автоматически сбрасываются в статус "ready"
- Используйте `/reset` для очистки всех данных при необходимости

## Лицензия

MIT
